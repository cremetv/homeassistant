schalte_alle_lichter_aus:
  alias: Schalte alle Lichter aus
  sequence:
  - wait_for_trigger:
    - platform: geo_location
      source: person.marcel
      zone: zone.home
      event: leave
  - service: light.turn_off
    data: {}
    target:
      entity_id: light.alle_lichter
  mode: single
  icon: mdi:lightbulb-group-off
